import Autocomplete from './components/Autocomplete/Autocomplete';

const App = () => {
  return (
    <div style={{ display: 'flex', padding: '2rem' }}>
      <Autocomplete />
    </div>
  );
};

export default App;
